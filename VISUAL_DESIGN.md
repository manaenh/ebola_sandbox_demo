# 视觉设计规范：EVD RESPONSE DIGITAL TWIN

版本：0.1（M1-1）

## 1. 设计命题

视觉目标是“应急指挥系统中的城市数字孪生”，不是通用数据看板，也不是卡通游戏。场景承担叙事，指标和树是场景的证据层。界面应在会议大屏上显得克制、精密、有空间深度，在笔记本上仍可操作。

关键词：深石墨、冷蓝光、医疗青、警戒琥珀、等距空间、玻璃层、扫描线、工程标注、可辨识人物。

## 2. 色彩系统

| Token | 值 | 用途 |
|---|---|---|
| `--ink-950` | `#071015` | 页面底色 |
| `--ink-900` | `#0B171D` | 面板底色 |
| `--ink-850` | `#102129` | 抬升表面 |
| `--line` | `rgba(137, 205, 218, .16)` | 网格与边界 |
| `--cyan` | `#59E1EC` | 主交互、正常在线 |
| `--teal` | `#64E6C3` | 规范控制、完成 |
| `--amber` | `#FFB45B` | 推演身份、待决策、警戒 |
| `--coral` | `#FF726F` | 暴露/污染事件，不等同“错误” |
| `--text` | `#EAF6F7` | 主要文字 |
| `--muted` | `#8FAAB2` | 次要文字 |

真实背景使用 cyan/blue 系；虚构推演身份使用 amber。风险颜色只表达当前态势，不给选择贴道德标签。

## 3. 排版

- 中文 UI：`Inter, "Noto Sans SC", "Microsoft YaHei", sans-serif`，依赖系统字体以支持离线。
- 数字/英文标签：`"DIN Alternate", "Bahnschrift", Inter, sans-serif`。
- 产品名称、场景名称、事件名称、人物姓名和动作说明必须以中文作为第一视觉层级；英文只能作为字号更小、对比度更低的对应副标签。
- 不在展示界面暴露演练编号、内部状态、开发标签或无法被参与者直接理解的系统术语。
- 标题避免超粗；使用字距、大小写英文眉题和细线建立技术感。
- 大屏基准：正文不小于 14px，关键数字 28—36px，时间 20—24px。

## 4. 布局与响应

- 1920×1080：最大内容宽度不锁死，舞台优先占用空间。
- ≥1280px：主舞台与任务侧栏双列，底部情报坞横向。
- 900—1279px：侧栏略窄，指标允许两行。
- <900px：降级为纵向阅读；不是本阶段主要设备，但不破版。
- 关键决策按钮始终在首屏或侧栏固定区域可见。

## 5. 2.5D 场景语法

### 5.1 等距空间

- 使用 SVG viewBox 构建固定逻辑坐标；CSS 自适应容器。
- 地面为菱形等距网格，墙面采用顶部/左侧/右侧三色体积关系。
- 家具、分诊台、座椅、隔离门都有投影和接触阴影，不使用扁平图标替代。
- 空间分区：候诊区、分诊区、临床走廊、隔离入口；通过地面色块和工程标注识别。

### 5.2 人物

- 人物由头、躯干、手臂、腿、衣着层、岗位符号和地面阴影组成，比例偏写实的简化矢量，而非圆头玩偶。
- 使用统一 `CharacterAvatar` 角色语法，支持患者、护士、医生、疾控、急救、保洁、家属和普通公众；类别通过轮廓、服装、姿态、胸前符号与克制色彩共同编码。
- 固定角色识别：周启航为深蓝民用外套、旅行背包、身体前倾/护腹姿态和暖色状态环；沈洁为灰绿外套；周可为较小体型与黄绿识别色。
- 护士使用青绿色 scrub、V 领和医疗岗位符号；普通候诊人员降低饱和度与对比度，但保留独立人体轮廓。
- 关键角色使用连接到人物本体的半持续姓名/角色/状态标牌；病例编号只出现在详细信息中。
- 角色只在热点激活时显示完整浮签，避免舞台拥挤。

### 5.3 M1-1 动画

- 初始：周启航轻微不稳/呼吸；扫描环低频脉冲；候诊屏有克制变化。
- A 路径：隔离门 cyan 开启，路径线亮起，周启航沿路径平移；随后锁定隔离态。
- B 路径：时间推进后出现一次性污染事件，地面 coral 警戒区扩展，人物状态改为“待评估”；不得用夸张液体或恐怖表现。
- 动画 180—900ms；环境脉冲可循环，但必须低对比。
- `prefers-reduced-motion: reduce` 时移除位移和循环，仅保留状态切换。

## 6. 组件视觉

- Panel：1px 半透明边、12—16px 圆角、轻微内高光；避免大面积毛玻璃模糊。
- Metric：小型英文眉题 + 中文标签 + 等宽数字 + 来源/状态微标。
- Decision card：两个方案同等面积和视觉权重；差异用行动描述，不预告结果色。
- Timeline：使用单一时间方向，分别显示日期、时刻、事件名和“已发生/当前/待发生”；不得用分支结构表达时间。
- Decision tree：当前事件为根节点，A/B 为同级并行子树，每个方案向下连接独立结果；所选边高亮，未选分支保留但降低强调，未揭示结果显示遮罩形态。
- Exposure chain：节点表达对象，带箭头连接线表达感染、有效暴露、一般接触或未揭示关系；关系标签位于连接线上方并与线保留间距。
- Status chip：明确区分 `真实背景`、`虚构桌面推演`、`源数据`、`模拟假设`。

## 7. 交互反馈

- 热点可通过鼠标、键盘 Enter/Space 激活；焦点环使用 cyan。
- 选择关键人物时，只增强该人物的亮度、轮廓环和对应身份标牌；信息卡必须与同一人物共享强调色，避免身份歧义。
- 选择前只显示行动内容；提交后才显示时间与数字后果。
- 场景变化和数字变化通过同一结果条说明，不让用户自己猜测因果。
- “重置 M1-1”明确会清除当前路径；后续“比较路径”在独立复盘视图处理。

## 8. 可访问性与演示可靠性

- 正文对比度目标 WCAG AA；颜色不是唯一状态编码。
- SVG 人物热点具备 `aria-label`；装饰网格从辅助技术隐藏。
- 无外部字体、地图瓦片、图标 CDN 或在线图片。
- 关键场景不用 Canvas-only，保证缩放清晰、DOM 可测和无障碍标签。
- 动画不闪烁；警报不使用高频红色闪动。

## 9. 图表原则

- 仅当关系不能由短文本清晰表达时使用图表。
- 传播链用最小节点链；接触者分类用条形/堆叠而非饼图；趋势必须有时间轴和来源。
- M1-1 不引入 ECharts/D3，避免为单节点增加依赖；SVG 足够完成舞台和树。
