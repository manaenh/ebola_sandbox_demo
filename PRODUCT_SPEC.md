# 埃博拉输入性疫情应急响应数字孪生推演：产品规格

版本：0.2（Module 1 可玩序列）
受众：南方科技大学演示与桌面推演参与者  
主语言：中文  
目标设备：笔记本电脑、16:9 大型会议显示屏  

## 1. 产品定位

本产品是一套离线优先、确定性运行的埃博拉输入性疫情桌面推演系统。它把应急指挥态势、2.5D 数字孪生场景和动态决策树同步在同一界面中，使用户观察事件、作出操作上都可成立的选择，并看到选择对暴露、追踪、资源与舆情的后续影响。

它不是疾病预测工具、临床决策支持系统或真实事件指挥系统。深圳市、人物、机构、航班、病例及推演数值均为虚构；真实世界 Bundibugyo 病毒病背景必须以独立视觉层呈现，并显示来源与数据截止日期。

## 2. 内容边界与标识

### 2.1 两个不可混淆的内容域

| 内容域 | 标签 | 用途 | 视觉规则 |
|---|---|---|---|
| 真实世界背景 | `REAL_WORLD` / `真实疫情背景` | 疾病基础知识、2026 年 Bundibugyo 疫情背景 | 蓝色信息带；必须显示来源和 `asOf` |
| 深圳桌面推演 | `TABLETOP_SIMULATION` / `虚构桌面推演` | 病例、事件、决策与演算 | 琥珀色 SIMULATION 标识常驻；不得暗示真实发生 |

真实背景基线采用用户提供 PDF 的 2026-07-30 截止数据：DRC 确诊 3,605 例、死亡 1,587 例、粗病死率 44%。其核心数值已与 WHO DON 614 核对。后续数据更新不自动改变本地演示包，必须经内容版本更新。

### 2.2 数字来源等级

- `source`：源桌面脚本或经核验的一手真实来源直接给出。
- `derived`：只由 `source` 数值确定性计算，保存计算说明。
- `assumption`：压力测试或产品演算假设，必须在 UI 标为“模拟假设”。
- `unknown`：不展示为数字，可展示“待核实”。

## 3. 用户与核心任务

- 演练主持人：启动/重置推演、控制节奏、投屏讲解、比较路径。
- 部门参与者：阅读事件注入，提交“四句话报告”，选择处置策略。
- 观察者/评估者：查看实时指标、传播链、时间线和分支证据。

核心任务流程：进入背景简报 → 开始引导推演 → 观察场景和风险线索 → 在两个可执行方案中选择 → 观看场景和指标同步变化 → 沿已揭示分支继续 → 完成复盘、时间线重放和反事实比较 → 导出报告。

## 4. 信息架构

### 4.1 一级导航

1. `背景简报`：真实世界流行病学与疾病知识，和推演明确隔离。
2. `联合指挥`：默认工作区；态势、场景、决策树同步。
3. `复盘评估`：最终评分、证据链、回放、反事实比较、报告。

Module 1 版本中，`联合指挥`可连续完成急诊分诊、患者沟通和首次报告；复盘入口仍显示建设状态而不伪造内容。

### 4.2 联合指挥工作区

- 顶栏：产品身份与三个一级导航；不重复展示事件时间或系统状态。
- 指标带：只显示当前事件有叙事意义的 3—4 项指标，并随事件演进。
- 主舞台（约 65% 宽）：2.5D 等距数字孪生场景。
- 任务侧栏（约 35% 宽）：事件注入、证据线索、选择、确定性态势摘要。
- 底部情报坞：传播链、事件时间线、动态决策树，可切换。

## 5. 三视图同步契约

所有视图只读取同一 `SimulationState`，不维护互相独立的业务状态。

1. 用户提交决策。
2. reducer 验证当前节点和可用选项。
3. 追加不可变决策记录与事件日志。
4. 应用指标 effect、推进模拟时间、触发 scene cues。
5. 只揭示已发生节点、所选边和即时可见后果。
6. 命令视图、数字孪生和树在同一渲染周期更新。

## 6. Module 1 可玩序列验收范围

### 6.1 初始态

- 时间：2026-08-05 10:42。
- 地点：深圳市中心医院急诊分诊区（虚构）。
- 周启航可见：高热、乏力、腹泻，仅陈述“非洲出差”。
- 候诊区已有 18 人。
- 指标显示：确诊 0、疑似 1；其余未完成评估时显示待核实，不用虚构数字补齐。
- 场景热点：患者、分诊台、候诊区、隔离通道。点击可查看角色/空间信息。

### 6.2 决策

两个选项不得标为正确/错误：

- A `立即升级流行病学问诊`：具体国家/日期/体液暴露史；佩戴口罩、限制陪同并引导隔离。
- B `先完成急诊基础评估`：在分诊区完成生命体征和基础检查，同时补充旅行史并准备隔离空间。

### 6.3 结果

- A：11:05 隔离；公共候诊区停留 23 分钟。
- B：12:00 隔离；公共候诊区停留 78 分钟；发生一次呕吐；新增需评估人员 21 人。
- 结果揭示后，场景、指标、时间线和决策树必须一致。
- 允许“重置节点”比较另一条路径，但默认不在作答前揭示另一条路径后果。

### 6.4 视觉验收

- 1366×768 和 1920×1080 下核心操作无需水平滚动。
- 角色具有稳定的姓名/岗位/颜色/轮廓识别，不以圆点替代人物。
- 2.5D 场景具有墙体、地面、家具、人物层级和空间动线。
- 动画尊重 `prefers-reduced-motion`。
- 大屏正常观看距离下，主要指标与决策文本清晰。

### 6.5 连续事件与完成态

- M1-1 结果揭示后以“继续推演”进入 11:12 患者拒绝事件，保留医院场景和既有分支状态。
- M1-2 记录患者是否配合、沟通耗时、采样是否可继续，以及短视频是否使舆情风险提前出现；不虚构社交媒体传播数值。
- M1-3 于 11:20 注入。先行快报路径记录 15 分钟内报告和疾控 12:00 前到场；等待检验路径记录整体延迟 2 小时 10 分钟。
- 三次决策必须写入同一状态和路径；决策树、时间线、指标与场景共同读取该状态。
- 三次二选一决策形成 8 条可区分的持久结果；不同结果来自暴露控制、患者合作与外部响应状态，不使用任意评分或虚构感染人数。
- 早期选择的物理后果按实际时刻执行：延迟分诊路径在 11:12 和 11:20 仍位于急诊区域，延迟隔离期间发生一次呕吐暴露并新增需评估 21 人，12:00 才完成隔离。引擎内部用于排序的呕吐时刻属于 `SIMULATION_ASSUMPTION`，不得作为源事实展示。
- M1-2 风险沟通路径在 11:12 进入“沟通进行中”，约 11:27 才执行患者配合后果；因此 11:20 的首次报告与患者沟通过程并行。
- 同一后续事件依据既有状态生成条件化叙事；决策树表达“决策→即时后果→下一共同事件”的分流与汇流。
- 完成 M1-3 后停在 Module 1 完成态，提供“返回全局态势”；Module 2 由城市态势中的下一任务显式启动，不自动跳转或重置状态。

### 6.6 深圳全局态势与现场推演

- `联合指挥`默认进入全局态势，并以轻量切换进入现场推演；两个视图共享同一个 `SimulationState`，切换不得重置进度。
- 城市位置目录包含深圳机场、机场交通、家庭、便利店/社区、网约车、市中心医院、市疾控机构，以及未启用的定点医院和实验室位置。
- 城市图只表达经过、待调查、体液暴露事件、疑似病例所在地与响应机构，不使用疫情热力图、区级感染色块、任意 Rt 或推测病例数。
- 市中心医院节点读取 Module 1 的暴露时长、需评估工作量和沟通信号；市疾控节点读取首次报告与响应延迟状态。
- 点击市中心医院进入现有 2.5D 现场；Module 1 完成后可返回全局态势并查看当前路径形成的宏观状态。
- 地图由 MapLibre GL JS 渲染本地深圳边界、区界与区域陆地数据，支持缩放、平移和倾斜且不依赖在线地图 API；演练机构坐标不宣称为真实地址或精确轨迹坐标。

### 6.7 转运、采样与实验室确证

- Module 2 连续包含转运请求、采样争议、实验室初筛阳性与复核确证，并继承 Module 1 的暴露控制、患者合作、舆情和响应延迟状态。
- 转运与采样分别使用可复用的 2.5D 场景；初筛和复核使用较轻的实验室/指挥状态视图。
- 转运路径区分备用专业能力与等待常备车辆；等待路径记录两次新增呕吐清理事件和处置压力，不推导新增感染。
- 采样路径区分规范最小必要采样与常规多项目流程；缺陷路径记录外包装模拟污染、暴露调查和 4 小时实验室延迟。
- 初筛阳性后的选择分别形成 18:00 前首批高风险接触者查找 92% 或 61% 的源脚本结果；复核确证将病例状态更新为确诊 1 例，并启动联合指挥与每日风险评估。
- 深圳态势图渐进显示患者转运、标本转运与响应协同网络；三类路线不表示疾病传播。

### 6.8 接触者追踪与 21 天管理

- Module 3 继承 Module 1–2 的评估工作量、信息质量、响应/实验室延迟以及 92%/61% 前序查找进展，不重置病例或工作状态。
- 首轮名单以 126 人聚合调查指挥台表达；家庭 2、同行 2、航班 39、医院 24、交通与社区 59 均是调查对象，不是密切接触者或感染者数量。
- 航班视图依据症状时序、座位、服务行为与体液暴露完成 7 高风险、18 中风险、39 低风险观察的源支持分类，其余 234 人是确定性差额，不是新的流行病学估计。
- 失联人员使用宏观跨区域协查视图；具体目的地不虚构为源事实。24 小时路径中的 12 人聚餐发生于无症状期，不形成有效传播事件。
- 模块完成后建立个体化 `ContactMonitoring` 记录：最后暴露时间、监测起止时间、个体当前监测日、监测状态与症状告警。UI 只显示“已进入 21 天健康监测阶段”，不存在全局 Day 1/21 时钟。
- 地图镜头由 `EventMapContext` 投影当前事件：支持单地点、路线、多地点、全市与跨区域五种语义，不再硬编码默认回到市中心医院。

## 7. 后续完整产品能力

- 引导式主持模式与自由探索模式。
- 全部 21 个事件节点和场景状态。
- 分支回放、书签和时间轴拖拽。
- 反事实指标对照，不把结果简化成“对/错”。
- 基于日志的确定性态势摘要与演后报告。
- 静态部署；演示核心不依赖 API、账户或网络。

## 8. 非目标

- 本阶段不实现 LLM、多人实时协作、真实病例接入、远程数据库或临床建议。
- 本阶段不实现 Module 4 及之后的交互，只保留 Module 3 的监测基础设施供后续模块消费。
- 不以随机传播模型替代源脚本的确定性结果。

## 9. 内容完整性与已知口径差异

- 文档总表写模拟疫情为 2026-08-03 至 09-16；M7-3 终止建议实际为 09-18 09:00。产品时间线保留事件原始日期，并注记总表差异。
- M3-1 的 126 人是当时“首轮需调查人员”；最终接触者表 16+23+42+87=168 人是全程累计分类，不共享同一时点或分母。
- M1-1 延误分支新增 21 名“需评估人员”，不得自动等同于接触者或密切接触者。
- “预计续发病例 3 例（范围 1—6）”只属于延误路径压力测试模型，不是现实预测。
